[build]
  command = "npm run build"
  publish = "dist"
  # Temporarily commenting out functions to bypass permission issues
  # functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Temporarily commenting out functions config to bypass permission issues
# [functions]
#  node_bundler = "esbuild" 